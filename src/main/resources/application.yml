spring:

  flyway:
    baseline-on-migrate: true
    enabled: true
    locations: classpath:/db/migration
    sql-migration-suffixes: sql


  application:
    name: movie15

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true

jwt:
  secret: ${JWT_SECRET_KEY}
  expiry-millis: 1800000  # 30분

cloud:
  aws:
    s3:
      bucket: codesix
    credentials:
      access-key: ${ACCESS_KEY}
      secret-key: ${SECRET_KEY}
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false

payment:
  toss:
    secretKey: test_sk_PBal2vxj81jQ4g5GoEPk35RQgOAN
    clientKey: test_ck_ALnQvDd2VJ2l5kawRB4vVMj7X41m
    successUrl: "http://localhost:8080/api/v1/payments/toss/success"
    failUrl: "http://localhost:8080/api/v1/payments/toss/fail"
    url: "https://api.tosspayments.com/v1/payments/"

  # Mail
  mail:
    host: smtp.gmail.com # SMTP 서버 호스트 Gmail 사용
    port: 587 # SMTP 서버 포트
    username: ${MAIL_ACCOUNT}
    password: ${MAIL_PASSWORD}
    protocol: smtp # 메일 전송 프로토콜
    properties:
      mail:
        smtp:
          auth: true # SMTP 서버 인증 활성화
          starttls:
            enable: true # TLS 암호화 활성화
        debug: true # 디버그 모드 활성화 (로그 출력)

tmdb:
  api-key: ${MDB_API_KEY}
  base-url: "https://api.themoviedb.org/3"

  # rabbitmq
  # docker 를 이용해서 rabbitmq 실행
  # docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:management -> rabbitmq 띄우기

  # delay-message 플러그인 다운로드 => https://github.com/rabbitmq/rabbitmq-delayed-message-exchange

  # 플러그인 카피 => 경로 각자 바뀔수있으니 경로확인
  # docker cp /Users/kimmyungho/Downloads/rabbitmq_delayed_message_exchange-4.0.2.ez \
  # rabbitmq:/plugins/

  # 도커 내부 접속
  # docker exec -it rabbitmq bash

  # 복사한 플러그인 활성화
  # rabbitmq-plugins enable rabbitmq_delayed_message_exchange

  # rabbitmq-plugins list     => 플러그인 확인
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    dynamic: true  # RabbitMQ 서버가 동작하지 않아도 실행 가능

